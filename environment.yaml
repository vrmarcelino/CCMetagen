channels:
  - conda-forge
  - bioconda
dependencies:
  - ete3
  - pandas
  - kma >=1.6.15  # mapstat parsing (fParseKMA.parse_mapstat_header) is tested against this and later; older KMA also works but is untested here
  - krona
